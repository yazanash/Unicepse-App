<Window x:Class="Uniceps.Views.AuthView.AddUserViewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Uniceps.Views.AuthView"
        mc:Ignorable="d"
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
   FlowDirection="RightToLeft"
        Background="{DynamicResource Brush.Background}"
        Title="AddUserViewWindow" Height="450" Width="500">
    <Border  >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="1" >

                <Border Style="{DynamicResource CardBorderStyle}" Grid.Column="0"  Grid.Row="0"  Padding="5"  Margin="5">
                    <DockPanel >
                        <DockPanel DockPanel.Dock="Top" >
                            <TextBlock Text="معلومات المستخدم" Foreground="{DynamicResource Brush.Foreground}" FontSize="24" DockPanel.Dock="Left"/>
                        </DockPanel>
                        <Grid >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="5">
                                <TextBlock Text="اسم المستخدم"  Foreground="{DynamicResource Brush.Foreground}"
                                Margin="0 0 0 8"
                                FontSize="18"/>
                                <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource textBoxStyle}"/>

                            </StackPanel>

                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="5">
                                <TextBlock Text="كلمة المرور"  Foreground="{DynamicResource Brush.Foreground}"
                                Margin="0 0 0 8"
                                FontSize="18"/>
                                <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource textBoxStyle}"/>


                            </StackPanel>
                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="5">
                                <TextBlock Text="اسم صاحب الحساب"  Foreground="{DynamicResource Brush.Foreground}"
                                Margin="0 0 0 8"
                                FontSize="18"/>
                                <TextBox Text="{Binding OwnerName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource textBoxStyle}"/>

                            </StackPanel>

                            <StackPanel Grid.Row="1" Grid.Column="1" Margin="5">
                                <TextBlock Text="المسمى الوظيفي"  Foreground="{DynamicResource Brush.Foreground}"
                                Margin="0 0 0 8"
                                FontSize="18"/>
                                <TextBox Text="{Binding Position, UpdateSourceTrigger=PropertyChanged }" Style="{StaticResource textBoxStyle}"/>

                            </StackPanel>
                            <StackPanel Grid.Row="2" Grid.Column="0" Margin="5">
                                <TextBlock Text="الدور" 
                                Margin="0 0 0 8" Foreground="{DynamicResource Brush.Foreground}"
                                FontSize="18"/>
                                <ComboBox Style="{DynamicResource ComboBoxStyle1}" 
                                         ItemsSource="{Binding RolesList}"
                                   SelectedItem="{Binding RoleItem,UpdateSourceTrigger=PropertyChanged}"
                                   ItemContainerStyle="{DynamicResource ComboBoxItemContainerStyle1}">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding RoleName}" FontSize="12"   />
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>

                                </ComboBox>


                            </StackPanel>


                            <StackPanel Orientation="Horizontal" Grid.Row="4" Grid.Column="1" Margin="0 20 0 0" HorizontalAlignment="Right">
                                <Button Command="{Binding SubmitCommand}" Style="{StaticResource FormButton}" Width="200" Grid.Column="0" Grid.Row="4"  Content="حفظ"  HorizontalAlignment="Right">
                                </Button>
                            </StackPanel>
                        </Grid>

                    </DockPanel>
                </Border>


            </Grid>
        </Grid>
    </Border>
</Window>
