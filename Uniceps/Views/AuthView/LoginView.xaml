<UserControl x:Class="Uniceps.Views.AuthView.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Uniceps"
             xmlns:icons="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             xmlns:custom="clr-namespace:LoadingSpinnerControl;assembly=LoadingSpinnerControl"
             FlowDirection="RightToLeft"
               PreviewKeyDown="UserControl_PreviewKeyDown"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>


    </UserControl.Resources>
    <!--<Border CornerRadius="20" FlowDirection="RightToLeft">
        <Border.Background>
           gym_backgroud
        </Border.Background>
        <Border CornerRadius="20" FlowDirection="RightToLeft" Opacity="0.95" Background="#EEE">


            <Border Background="#fff" Width="250" Height="350" Margin="0 10 0 10" Padding="10" CornerRadius="20" >
                <StackPanel Orientation="Vertical">
                    <Border Width="80" Height="80"
                            Margin="10 0 0 0"
                                CornerRadius="20"
                            FlowDirection="LeftToRight"
                            
                          Background="#fff" BorderThickness="0" BorderBrush="#C14E00">
                        <Rectangle>
                            <Rectangle.Fill>
                                <ImageBrush Stretch="Fill" ImageSource="../../Resources/Assets/p1.jpg"/>
                            </Rectangle.Fill>
                        </Rectangle>
                    </Border>

                    <StackPanel Grid.Row="2" Grid.Column="0" Margin="5">

                        <TextBlock Text="اسم المستخدم" 
                                   Foreground="#993300"
                                   Margin="0 0 0 5"
                                   FontSize="13"/>
                        <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource textBoxStyle}"/>

                    </StackPanel>
                    <StackPanel Grid.Row="3" Grid.Column="1" Margin="5 5">
                        <TextBlock Text="كلمة المرور" 
                                   Foreground="#993300"
                                   Margin="0 0 0 5"
                                   FontSize="13"/>
                        <PasswordBox Style="{StaticResource passwordStyle}"/>
                    </StackPanel>
                    <Button Command="{Binding AuthCommand}" Style="{StaticResource FormButton}" Margin="0 10" HorizontalAlignment="Center">
                       
                        <Grid  Grid.Row="4">
                            <custom:LoadingSpinner
                IsLoading="{Binding IsLoading}"
                Diameter="30" Thickness="3" Color="#fff" />
                            <Grid Visibility="{Binding IsLoading ,Converter={StaticResource InverseBooleanToVisibalityConverter}}">
                               

                                <TextBlock Text="تسجيل دخول" Visibility="{Binding IsLoading,Converter={StaticResource InverseBooleanToVisibalityConverter}}"/>
                                
                            </Grid>
                        </Grid>
                    </Button>
                </StackPanel>
            </Border>
        </Border>

    </Border>-->

    <Border Width="800" Height="450" CornerRadius="10" BorderThickness="1" BorderBrush="#787878">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" FlowDirection="RightToLeft" Grid.Column="0" Panel.ZIndex="1" VerticalAlignment="Top" Margin="10" HorizontalAlignment="Left">
                <icons:PackIconMaterial Kind="Close" Panel.ZIndex="2" MouseDown="Close_MouseUp" Width="15" Margin="5" Height="15" Foreground="{DynamicResource Brush.Foreground}"/>
                <icons:PackIconMaterial Kind="WindowMinimize" Panel.ZIndex="2" MouseDown="Minimize_MouseUp" Width="15" Margin="5" Height="15" Foreground="{DynamicResource Brush.Foreground}"/>

            </StackPanel>
            <Border CornerRadius="10 0 0 10" Grid.Column="1" FlowDirection="LeftToRight">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="{DynamicResource Color.Surface}" Offset="0"/>
                        <GradientStop Color="{DynamicResource Color.Surface}" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <Canvas>
                        <Polygon Points="0,20 230,140 0,270" Fill="{DynamicResource Brush.Primary}"/>
                        <Polygon Points="100,400 200,370 180,450" Fill="{DynamicResource Brush.Primary}"/>
                        <Ellipse Margin="250 400 0 0" Height="40" Width="40" Fill="{DynamicResource Brush.Primary}" />
                        <Ellipse Margin="50 350 0 0" Height="20" Width="20" Fill="{DynamicResource Brush.Primary}" />
                    </Canvas>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Uniceps" Style="{StaticResource textTitle}" />
                        <TextBlock Text="login and join our verse" Style="{StaticResource normalText}" />
                        <TextBlock Text="Welcome to TrioVesre" Style="{StaticResource normalText}" />
                    </StackPanel>
                </Grid>
            </Border>


            <Border Background="{DynamicResource Brush.Surface}" CornerRadius="10 0 0 10" Grid.Column="0">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="اهلا بك من جديد" Style="{StaticResource textTitle}"/>
                    <StackPanel Orientation="Horizontal" Margin="0 20 " HorizontalAlignment="Center">
                       
                    </StackPanel>
                    <TextBlock Text="تسجيل الدخول" Style="{StaticResource normalText}" Margin="0 10 0 15" Foreground="{DynamicResource Brush.Foreground}"/>
                    <Border Style="{DynamicResource CardBorderStyle}" Padding="5 0" Margin=" 70 7">
                        <Grid Margin="9 0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <icons:PackIconMaterial Kind="AccountOutline" VerticalAlignment="Center" Foreground="{DynamicResource Brush.Foreground}" Width="20" Height="20"/>
                            <!--<TextBlock Text="ادخل اسم المستخدم" Style="{StaticResource hintText}"
                                      x:Name="lbl_username"
                                       MouseDown="email_MouseDown"/>-->
                            <!--<TextBox x:Name="email_txt"
                                     Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}"
                                     TextChanged="email_txt_TextChanged" TabIndex="0"
                                     Style="{StaticResource loginTextbox}" />-->
                            <ComboBox Background="Transparent" BorderThickness="0"
                                      ItemsSource="{Binding Users}"
                                      SelectedItem="{Binding UserName}"
                                      Margin="10 0 0 0 " Style="{DynamicResource ComboBoxStyle1}" Grid.Column="1">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding }" FontSize="18" Foreground="{DynamicResource Brush.Foreground}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </Grid>
                    </Border>

                    <Border Style="{DynamicResource CardBorderStyle}" Margin=" 70 7">
                        <Grid Margin="9 7">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <icons:PackIconMaterial Kind="LockOutline" Foreground="{DynamicResource Brush.Foreground}" Width="20" Height="20"/>
                            <TextBlock x:Name="lbl_password" Text="ادخل كلمة المرور" Style="{StaticResource hintText}"
                                      MouseDown="password_MouseDown" />
                            <PasswordBox x:Name="password_txt" TabIndex="1"  PasswordChanged="password_txt_PasswordChanged" Style="{StaticResource loginPassTextbox}" />
                        </Grid>
                    </Border>
                    <Button x:Name="login_btn" Content="تسجيل دخول" Command="{Binding LoginCommand}" Style="{StaticResource signinBtn}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
       
   
    
</UserControl>
