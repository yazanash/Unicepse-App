<UserControl x:Class="Uniceps.Views.TrainerViews.TrainerDues"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Uniceps.Views.TrainerViews"
            xmlns:ctrl="clr-namespace:Uniceps.Controls"
             xmlns:cdr="clr-namespace:Uniceps.utlis.Components"
             mc:Ignorable="d" 
             FlowDirection="RightToLeft"
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks" 
             d:DesignWidth="800">
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{DynamicResource StaticCardBorderStyle}" Padding="10" CornerRadius="8" Margin="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Icon:PackIconMaterial Kind="CalendarClock" Foreground="{DynamicResource Brush.Primary}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="تاريخ تقرير المستحقات:" Foreground="{DynamicResource Brush.Foreground}" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <DatePicker SelectedDate="{Binding ReportDate, UpdateSourceTrigger=PropertyChanged}" Width="180" Height="35" VerticalContentAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button Command="{Binding LoadMounthlyReport}" Style="{StaticResource AddBtn}" Margin="10,0" Padding="15,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="تحضير التقرير" FontWeight="Bold"/>
                            <Icon:PackIconMaterial Kind="Refresh" Width="14" Height="14" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button Command="{Binding PrintCommand}" Style="{StaticResource EditBtn}" Padding="15,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="طباعة" FontWeight="Bold"/>
                            <Icon:PackIconMaterial Kind="Printer" Width="14" Height="14" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <ContentControl Grid.Row="1" Content="{Binding TrainerMounthlyReportViewModel}">
            <ContentControl.ContentTemplate>
                <DataTemplate>
                    <cdr:EmployeeAccountantCard/>
                </DataTemplate>
            </ContentControl.ContentTemplate>
        </ContentControl>
    </Grid>
</UserControl>
