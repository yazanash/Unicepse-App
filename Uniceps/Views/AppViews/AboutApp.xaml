<UserControl x:Class="Uniceps.Views.AppViews.AboutApp"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Uniceps.Views.AppViews"
             mc:Ignorable="d" 
             FlowDirection="RightToLeft"
               xmlns:cs="clr-namespace:Uniceps.utlis.Controls"
             d:DesignHeight="450" d:DesignWidth="800">
    <Border CornerRadius="10"   Padding="10" >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>

            </Grid.RowDefinitions>

            <Border  Grid.Column="1"  Grid.Row="1"  Padding="0 0 0 5"  Margin="5">
                <Grid Grid.Row="1" >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>
                    <Border  Grid.Column="0" HorizontalAlignment="Left"  Grid.Row="1"  Padding="5"  Margin="5">
                        <DockPanel HorizontalAlignment="Center"  >
                            <DockPanel DockPanel.Dock="Top" >
                                <TextBlock Foreground="{DynamicResource Brush.Foreground}" Text="معلومات التطبيق"  FontSize="24" DockPanel.Dock="Left"/>
                            </DockPanel>
                            <cs:AppCard/>

                        </DockPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
