<UserControl x:Class="Uniceps.Views.RoutineTemplateViews.RoutineComponent.ExercisesListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Uniceps.Views.RoutineTemplateViews.RoutineComponent"
             mc:Ignorable="d" 
                xmlns:icons="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:routinecomponent="clr-namespace:Uniceps.Views.RoutineTemplateViews.RoutineComponent"
    xmlns:cs="clr-namespace:Uniceps.Components"
   FlowDirection="RightToLeft"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="boolToVis"/>
        <Style x:Key="NewExercisesListBoxItemContainerStyle" TargetType="{x:Type ListBoxItem}">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="HorizontalContentAlignment" Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
            <Setter Property="VerticalContentAlignment" Value="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="IsSelected" Value="{Binding IsSelected,UpdateSourceTrigger=PropertyChanged}"/>
            <EventSetter Event="Selected" Handler="ListBoxItem_Selected"/>
            <EventSetter Event="Unselected" Handler="ListBoxItem_Unselected"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">

                        <Border  x:Name="Bd"  Margin="5" Style="{DynamicResource StaticCardBorderStyle}">
                            <DockPanel >
                                <Image Width="150"
            Source="{Binding ExerciseImage}"
             RenderOptions.BitmapScalingMode="HighQuality"
RenderOptions.EdgeMode="Aliased"
            Height="150" DockPanel.Dock="Top"/>
                                <StackPanel Orientation="Vertical" Margin="5" DockPanel.Dock="Top" VerticalAlignment="Center">
                                    <TextBlock Width="200" TextWrapping="Wrap" Text="{Binding ExerciseName}" Foreground="{DynamicResource Brush.Foreground}" Margin="0 0 0 5" HorizontalAlignment="Center" FontSize="14" />
                                    <TextBlock Text="هذا التمرين مضاف مسبقا" Foreground="{DynamicResource Brush.Foreground}" Visibility="{Binding IsChecked,Converter={StaticResource boolToVis}}" Margin="0 0 0 5" HorizontalAlignment="Center" FontSize="14" />
                                </StackPanel>

                            </DockPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="Bd" Value="{DynamicResource Brush.Card}"/>
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Selector.IsSelectionActive" Value="False"/>
                                    <Condition Property="IsSelected" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="Bd" Value="{DynamicResource Brush.Card}"/>
                                <Setter Property="BorderBrush" TargetName="Bd" Value="{DynamicResource Brush.Primary}"/>
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Selector.IsSelectionActive" Value="True"/>
                                    <Condition Property="IsSelected" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="Bd" Value="{DynamicResource Brush.Card}"/>
                                <Setter Property="BorderBrush" TargetName="Bd" Value="{DynamicResource Brush.Primary}"/>
                            </MultiTrigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="TextElement.Foreground" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Padding="5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0"  Padding="5" >
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="250" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>
      
                <TextBlock Text="صفحة اختيار التمارين" Foreground="{DynamicResource Brush.Foreground}" VerticalAlignment="Center" FontSize="18"/>
                <Border CornerRadius="{DynamicResource Radius.Small}" Margin="10 5"  Grid.Row="0"  Grid.Column="1"  VerticalAlignment="Top">
                    <cs:CustomTextBox Hint="ابحث هنا"  DataContext="{Binding SearchBox,UpdateSourceTrigger=PropertyChanged}"/>
                </Border>
                <StackPanel Grid.Row="0" Grid.Column="2" Margin="5" Orientation="Horizontal"  VerticalAlignment="Center">
                  
                    <ComboBox Style="{DynamicResource ComboBoxStyle1}" 
       ItemsSource="{Binding MuscleGroupList,UpdateSourceTrigger=PropertyChanged}" Tag="العضلة :"
       SelectedItem="{Binding SelectedMuscle,UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"
       ItemContainerStyle="{DynamicResource ComboBoxItemContainerStyle1}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" FontSize="12"   />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>
                <Button Style="{StaticResource AddBtn}" Margin="10 0" Grid.Column="3" Command="{Binding AddToRoutineCommand}">
                    <TextBlock Text="تم"/>
                </Button>
            </Grid>
        </Border>


        <Border Grid.Row="2">
            <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <ListView Background="Transparent" BorderThickness="0" SelectionMode="Multiple" Grid.Row="2" ItemsSource="{Binding ExercisesList}" ItemContainerStyle="{StaticResource NewExercisesListBoxItemContainerStyle}">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="5"/>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                </ListView>
            </Grid>
        </Border>



    </Grid>
    </Border>
</UserControl>
