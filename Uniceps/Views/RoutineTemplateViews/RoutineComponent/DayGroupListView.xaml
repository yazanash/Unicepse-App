<UserControl x:Class="Uniceps.Views.RoutineTemplateViews.RoutineComponent.DayGroupListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Uniceps.Views.RoutineTemplateViews.RoutineComponent"
             mc:Ignorable="d" 
              xmlns:Comp="clr-namespace:Uniceps.utlis.Components"
              xmlns:converters="clr-namespace:Uniceps.utlis.Converters"
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:icons="http://metro.mahapps.com/winfx/xaml/iconpacks"
             FlowDirection="RightToLeft"
             >
    <UserControl.InputBindings>
        <!-- زر Up يحرك العنصر المحدد للأعلى -->
        <KeyBinding Key="Up" Modifiers="Alt"
                Command="{Binding MoveUpCommand}"
                CommandParameter="{Binding SelectedDayGroup}" />

        <!-- زر Down يحرك العنصر المحدد للأسفل -->
        <KeyBinding Key="Down" Modifiers="Alt"
                Command="{Binding MoveDownCommand}"
                CommandParameter="{Binding SelectedDayGroup}" />
    </UserControl.InputBindings>
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="boolToVis"/>
        <converters:InverseBooleanToVisibalityConverter x:Key="InverseBooleanToVisibalityConverter"/>
    </UserControl.Resources>
    <Border Style="{DynamicResource StaticCardBorderStyle}">
        <Grid HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0">
                <TextBlock Text="الأيام :" Foreground="{DynamicResource Brush.Foreground}" VerticalAlignment="Center" Margin="0 0 8 0" FontSize="16"/>
                <Button Grid.Column="2" Grid.Row="1" Command="{Binding AddDayGroupCommand}" Style="{StaticResource IconBtn}">
                    <icons:PackIconMaterial Kind="Plus" />
                </Button>
              
            </StackPanel>
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                <Button  ToolTip="Alt + Up"
            Command="{Binding MoveUpCommand}" 
            CommandParameter="{Binding SelectedDayGroup}" Style="{DynamicResource BorderedIconBtn}">
                    <icons:PackIconMaterial Kind="ArrowUp"/>
                </Button>
                <Button  Margin="2 0"
            Command="{Binding MoveDownCommand}" ToolTip="Alt + Down "
            CommandParameter="{Binding SelectedDayGroup}"  Style="{DynamicResource BorderedIconBtn}">
                    <icons:PackIconMaterial Kind="ArrowDown"/>
                </Button>
                <Button Grid.Column="2" Grid.Row="1" Visibility="{Binding HasOrderChanged,Converter={StaticResource boolToVis}}" Command="{Binding SaveNewOrderCommand}" Style="{StaticResource BorderedIconBtn}">
                    <TextBlock Text="حفظ ترتيب الايام"/>
                </Button>
            </StackPanel>
            <ListView Background="Transparent" x:Name="DayGroupList"
                       PreviewKeyDown="ListView_PreviewKeyDown"
                      BorderThickness="0"  VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.ScrollUnit="Pixel" Grid.Row="2" Grid.Column="1" ItemsSource="{Binding DayGroups}" SelectedItem="{Binding SelectedDayGroup}" >
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel  Orientation="vertical"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemContainerStyle>
                    <Style TargetType="{x:Type ListBoxItem}">
                        <Setter Property="SnapsToDevicePixels" Value="True"/>
                        <Setter Property="Padding" Value="5"/>
                        <Setter Property="HorizontalContentAlignment" Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
                        <Setter Property="VerticalContentAlignment" Value="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="Focusable" Value="True"/>
                        <Setter Property="RenderTransform">
                            <Setter.Value>
                                <TranslateTransform />
                            </Setter.Value>
                        </Setter>
                        <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListBoxItem}">

                                    <Border  MinWidth="100" x:Name="Bd" Margin="5" Style="{DynamicResource StaticCardBorderStyle}">

                                        <StackPanel Orientation="Horizontal">

                                            <TextBlock FontSize="16" Foreground="{DynamicResource Brush.Foreground}" FontWeight="Bold"
                                   Text="{Binding DayGroupName,UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"  HorizontalAlignment="Center"></TextBlock>
                                            <CheckBox 
                                                      VerticalAlignment="Center"  Style="{StaticResource MenuTopBtn}" IsChecked="{Binding IsEditing,UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="left" Grid.Row="0" Grid.Column="2">

                                                <Icon:PackIconMaterial Kind="Pencil" Style="{StaticResource GridBtnIcon}"/>
                                            </CheckBox>
                                            <Button Command="{Binding DeleteCommand}" Style="{StaticResource IconBtn}" FontSize="12"  Grid.Column="0" Grid.Row="4"  HorizontalAlignment="Center">
                                                <Icon:PackIconMaterial Kind="TrashCan" Foreground="red" Style="{StaticResource GridBtnIcon}"/>
                                            </Button>
                                            <!-- Popup for Editing -->
                                            <Popup Opened="MyPopup_Opened"  StaysOpen="False" AllowsTransparency="True" Name="MyPopup" IsOpen="{Binding IsEditing,UpdateSourceTrigger=PropertyChanged}" PlacementTarget="{Binding RelativeSource={RelativeSource Self}}">
                                                <!-- Customize the popup content here -->
                                                <Border Style="{DynamicResource CardBorderStyle}">

                                                    <StackPanel Orientation="Vertical">
                                                        <StackPanel Grid.Row="0" Grid.Column="1" Margin="5">

                                                            <TextBox Name="EditTextBox" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource textBoxStyle}"/>


                                                        </StackPanel>
                                                        <Button Command="{Binding SubmitCommand}" Style="{StaticResource FormButton}" FontSize="12"  Grid.Column="0" Grid.Row="4"  Content="حفظ"  HorizontalAlignment="Center">
                                                        </Button>
                                                    </StackPanel>

                                                </Border>
                                               
                                            </Popup>
                                        </StackPanel>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <!--<EventTrigger RoutedEvent="Loaded">
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.X"
                                         From="10" To="0"
                                         Duration="0:0:0.5"
                                        />
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>-->
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <Setter Property="Background" TargetName="Bd" Value="{DynamicResource Brush.Card}"/>
                                        </MultiTrigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="Selector.IsSelectionActive" Value="False"/>
                                                <Condition Property="IsSelected" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <Setter Property="Background" TargetName="Bd" Value="{DynamicResource Brush.Card}"/>
                                            <Setter Property="BorderBrush" TargetName="Bd" Value="{DynamicResource Brush.Primary}"/>

                                        </MultiTrigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="Selector.IsSelectionActive" Value="True"/>
                                                <Condition Property="IsSelected" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <Setter Property="Background" TargetName="Bd" Value="{DynamicResource Brush.Card}"/>
                                            <Setter Property="BorderBrush" TargetName="Bd" Value="{DynamicResource Brush.Primary}"/>
                                            <!--<Setter Property="Visibility" TargetName="editbtn" Value="Visible"/>-->
                                        </MultiTrigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter Property="TextElement.Foreground" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
          
            </ListView>
            <Comp:CaptinUniTRainerForEmpty Visibility="{Binding HasData,Converter={StaticResource InverseBooleanToVisibalityConverter}}" Grid.Row="2" Title="لا يوجد ايام بعد" 
                        Caption="اضغط على زر + لاضافة يوم تدريبي"/>
        </Grid>

    </Border>

</UserControl>
