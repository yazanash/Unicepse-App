<UserControl x:Class="Uniceps.Views.RoutineTemplateViews.RoutineComponent.RoutineItemModelCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Uniceps.Views.RoutineTemplateViews.RoutineComponent"
             mc:Ignorable="d" 
             
             xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
            >
    <UserControl.Resources>

        <Style x:Key="SetModelsListBoxItemContainerStyle1" TargetType="{x:Type ListBoxItem}">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="HorizontalContentAlignment" Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
            <Setter Property="VerticalContentAlignment" Value="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <EventSetter Event="MouseMove" Handler="MouseMoveHandler"/>
            <EventSetter Event="DragOver" Handler="DragOverHandler"/>
            <EventSetter Event="Drop" Handler="ListBoxItem_Drop"/>
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">

                        <Border Style="{DynamicResource StaticCardBorderStyle}" x:Name="Bd"  Margin="5" >

                            <Grid >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>


                                <StackPanel Orientation="Horizontal" Grid.Column="0">
                                    <TextBlock FontSize="16" Foreground="{DynamicResource Brush.Foreground}" FontWeight="Bold"
                                        Text="{Binding RoundIndex ,UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"  HorizontalAlignment="Left"></TextBlock>
                                    <TextBlock FontSize="16" Foreground="{DynamicResource Brush.Foreground}" FontWeight="Bold"
          Text="-" VerticalAlignment="Center"  HorizontalAlignment="Left"></TextBlock>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Grid.Column="1" Margin="5">
                                    <TextBlock FontSize="16" Foreground="{DynamicResource Brush.Foreground}" FontWeight="Bold"
      Text="العد :" VerticalAlignment="Center"  HorizontalAlignment="Center"></TextBlock>

                                    <TextBlock FontSize="16" Foreground="{DynamicResource Brush.Foreground}" FontWeight="Bold"
      Text="{Binding Repetition,UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"  HorizontalAlignment="Center"></TextBlock>
                                </StackPanel>

                                <CheckBox  VerticalAlignment="Center"  Grid.Column="2"  Style="{StaticResource MenuTopBtn}" IsChecked="{Binding IsEditing,UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="left" Grid.Row="0" >
                                    <icon:PackIconMaterial Kind="Pencil" Style="{StaticResource GridBtnIcon}"/>
                                </CheckBox>
                                <!-- Popup for Editing -->
                                <Popup StaysOpen="False" AllowsTransparency="True" Name="MyPopup" IsOpen="{Binding IsEditing,UpdateSourceTrigger=PropertyChanged}" PlacementTarget="{Binding RelativeSource={RelativeSource Self}}">
                                    <!-- Customize the popup content here -->
                                    <Border Style="{DynamicResource CardBorderStyle}">

                                        <StackPanel Orientation="Vertical">
                                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="5">

                                                <TextBox Text="{Binding Repetition, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource textBoxStyle}"/>


                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Command="{Binding SubmitCommand}" Style="{StaticResource FormButton}" FontSize="12"  Grid.Column="0" Grid.Row="4"  Content="حفظ"  HorizontalAlignment="Right">
                                                </Button>
                                                <Button Command="{Binding DeleteCommand}" Margin="5" Style="{StaticResource IconBtn}" FontSize="12"  Grid.Column="0" Grid.Row="4" >
                                                    <icon:PackIconMaterial Kind="TrashCan" Foreground="Red"/>
                                                </Button>
                                            </StackPanel>

                                        </StackPanel>

                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="Bd" Value="{DynamicResource Brush.Card}"/>
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Selector.IsSelectionActive" Value="False"/>
                                    <Condition Property="IsSelected" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="Bd" Value="{DynamicResource Brush.Card}"/>
                                <Setter Property="BorderBrush" TargetName="Bd" Value="{DynamicResource Brush.Primary}"/>

                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Selector.IsSelectionActive" Value="True"/>
                                    <Condition Property="IsSelected" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="Bd" Value="{DynamicResource Brush.Card}"/>
                                <Setter Property="BorderBrush" TargetName="Bd" Value="{DynamicResource Brush.Primary}"/>
                            </MultiTrigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="TextElement.Foreground" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Border Style="{DynamicResource StaticCardBorderStyle}" >
        <Grid >
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Image Width="200" Grid.Row="0"
        Source="{Binding ExerciseImage,UpdateSourceTrigger=PropertyChanged}"
        RenderOptions.BitmapScalingMode="HighQuality"
        RenderOptions.EdgeMode="Aliased"
        Height="100" DockPanel.Dock="Top"/>
            <TextBlock Grid.Row="1" HorizontalAlignment="Center" Foreground="{DynamicResource Brush.Foreground}" Text="{Binding ExerciseName,UpdateSourceTrigger=PropertyChanged}" FontSize="16" FontWeight="ExtraBold"/>
           
           
                <StackPanel Orientation="Horizontal" Grid.Row="2">
                <TextBlock Text="المجموعات التدريبية" FontSize="14" Foreground="{DynamicResource Brush.Foreground}"/>
                    <Button Grid.Row="2" HorizontalAlignment="Center" Grid.Column="2" Command="{Binding AddSetModelCommand}" Style="{StaticResource IconBtn}">
                        <icon:PackIconMaterial Kind="Plus" HorizontalAlignment="Center"/>
                    </Button>
                </StackPanel>

            <ListView DragLeave="TodoItemList_DragLeave" Background="Transparent"
 DragOver="TodoItemList_DragOver" AllowDrop="True" x:Name="lvItems"  BorderThickness="0" VirtualizingPanel.ScrollUnit="Pixel"  Grid.Row="3" ItemsSource="{Binding SetModelItems}"  ItemContainerStyle="{StaticResource SetModelsListBoxItemContainerStyle1}">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Vertical"/>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                </ListView>
                
            
           
        </Grid>
    </Border>
</UserControl>
