<Window x:Class="Uniceps.Views.RoutineTemplateViews.RoutineComponent.ExercisesListViewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Uniceps.Views.RoutineTemplateViews.RoutineComponent"
        mc:Ignorable="d"
             xmlns:Converters="clr-namespace:Uniceps.utlis.Converters"
                     xmlns:icons="http://metro.mahapps.com/winfx/xaml/iconpacks"
 xmlns:routinecomponent="clr-namespace:Uniceps.Views.RoutineTemplateViews.RoutineComponent"
 xmlns:cs="clr-namespace:Uniceps.Components"
FlowDirection="RightToLeft"
        WindowState="Maximized"
        Background="{DynamicResource Brush.Background}"
        WindowStartupLocation="CenterScreen"
        Title="اختيار التمارين الرياضية" Height="450" Width="800">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="boolToVis"/>
        <Converters:EnumDisplayNameConverter x:Key="EnumToText"/>
        <Style x:Key="NewExercisesListBoxItemContainerStyle" TargetType="{x:Type ListBoxItem}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="IsSelected" Value="{Binding IsSelected,UpdateSourceTrigger=PropertyChanged}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate   TargetType="{x:Type ListBoxItem}">

                        <Border  x:Name="Bd"  Margin="5" Style="{DynamicResource StaticCardBorderStyle}">
                            <DockPanel >
                                <Image Width="150"
            Source="{Binding ExerciseImage}"
             RenderOptions.BitmapScalingMode="HighQuality"
RenderOptions.EdgeMode="Aliased"
            Height="150" DockPanel.Dock="Top"/>
                                <StackPanel Orientation="Vertical" Margin="5" HorizontalAlignment="Center" DockPanel.Dock="Top" VerticalAlignment="Center">
                                    <TextBlock HorizontalAlignment="Center" Width="200" TextAlignment="Center" TextWrapping="Wrap" Text="{Binding ExerciseName}" Foreground="{DynamicResource Brush.Foreground}" Margin="0 0 0 5"  FontSize="14" />
                                    <TextBlock Text="هذا التمرين مضاف مسبقا" Foreground="{DynamicResource Brush.Foreground}" Visibility="{Binding IsChecked,Converter={StaticResource boolToVis}}" Margin="0 0 0 5" HorizontalAlignment="Center" FontSize="14" />
                                </StackPanel>

                            </DockPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="Bd" Value="{DynamicResource Brush.Card}"/>
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Selector.IsSelectionActive" Value="False"/>
                                    <Condition Property="IsSelected" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="Bd" Value="{DynamicResource Brush.Card}"/>
                                <Setter Property="BorderBrush" TargetName="Bd" Value="{DynamicResource Brush.Primary}"/>
                                <Setter Property="BorderThickness" TargetName="Bd" Value="2"/>
                                <Setter Property="TextElement.Foreground" TargetName="Bd" Value="{DynamicResource Brush.Foreground}"/>
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Selector.IsSelectionActive" Value="True"/>
                                    <Condition Property="IsSelected" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="Bd" Value="{DynamicResource Brush.Card}"/>
                                <Setter Property="BorderBrush" TargetName="Bd" Value="{DynamicResource Brush.Primary}"/>
                                <Setter Property="TextElement.Foreground" TargetName="Bd" Value="{DynamicResource Brush.Foreground}"/>
                                <Setter Property="BorderThickness" TargetName="Bd" Value="2"/>
                            </MultiTrigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="TextElement.Foreground" TargetName="Bd" Value="{DynamicResource Brush.Foreground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="MyChipToggleStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="{DynamicResource Brush.Card}"/>
            <Setter Property="Foreground" Value="{DynamicResource Brush.Foreground}"/>
            <Setter Property="Padding" Value="10,5"/>
            
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border CornerRadius="15"  Margin="3"
                        Background="{TemplateBinding Background}" 
                        BorderBrush="{DynamicResource Brush.Border}" 
                        BorderThickness="1" 
                        Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="{DynamicResource Brush.Primary}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Padding="5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Style="{DynamicResource StaticCardBorderStyle}"  Grid.Column="1" Padding="5" >
                <StackPanel Orientation="Horizontal" Grid.Row="0">
                    <TextBlock Foreground="{DynamicResource Brush.Foreground}" FontSize="16" Text="قائمة التمارين الرياضية" VerticalAlignment="Center" FontWeight="Bold"/>
                    <Border Style="{DynamicResource CardBorderStyle}" Margin="5 0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" >
                        <TextBlock Foreground="{DynamicResource Brush.Foreground}" Margin="5 0" FontSize="14" Text="عدد النتائج" FontWeight="Bold"/>
                        <TextBlock Foreground="{DynamicResource Brush.Foreground}" FontSize="14" Text="{Binding Total,UpdateSourceTrigger=PropertyChanged}" FontWeight="Bold"/>
                    </StackPanel>
                    </Border>
                    <Border Style="{DynamicResource CardBorderStyle}" Margin="5 0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5 0">
                        <TextBlock Foreground="{DynamicResource Brush.Foreground}" Margin="5 0" FontSize="14" Text="المحدد" FontWeight="Bold"/>
                        <TextBlock Foreground="{DynamicResource Brush.Foreground}" FontSize="14" Text="{Binding SelectedCount,UpdateSourceTrigger=PropertyChanged}" FontWeight="Bold"/>
                    </StackPanel>
                    </Border>
                    <Button Style="{StaticResource AddBtn}" Margin="10 0" Grid.Column="3" Command="{Binding AddToRoutineCommand}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="اضافة"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>
            <Border  Style="{DynamicResource StaticCardBorderStyle}" Grid.Row="0"  Padding="10">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Foreground="{DynamicResource Brush.Foreground}" FontSize="16" Text="نظام الفلترة الذكي" FontWeight="Bold"/>
                        

                        <Button Style="{StaticResource IconBtn}" Margin="10 0" Grid.Column="3" Command="{Binding ClearFiltersCommand}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="الغاء الفلترة "/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </StackPanel>

            </Border>
            <Border  Style="{DynamicResource StaticCardBorderStyle}" Grid.Row="1"  Padding="10">
                <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">

                    <StackPanel>


                        <StackPanel Grid.Row="0" Grid.Column="0"  Margin="5" Orientation="Vertical" >
                            <TextBlock Foreground="{DynamicResource Brush.Foreground}" FontSize="14" Text="تكنيك الحركة"/>
                            <ComboBox Width="200" Style="{DynamicResource ComboBoxStyle1}" 
  ItemsSource="{Binding MechanismList,UpdateSourceTrigger=PropertyChanged}" 
  SelectedItem="{Binding SelectedMechanism,UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"
  ItemContainerStyle="{DynamicResource ComboBoxItemContainerStyle1}"
>
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Converter={StaticResource EnumToText}}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>
                        <TextBlock Foreground="{DynamicResource Brush.Foreground}" FontSize="14" Text="المجموعات العضلية" Margin="0,5"/>
                        <ListBox ItemsSource="{Binding MuscleGroupList}" 
                 SelectionMode="Multiple"
                 Background="Transparent" BorderThickness="0">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="2" IsItemsHost="True"/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <ToggleButton Content="{Binding Name}" 
                                             Style="{DynamicResource MyChipToggleStyle}" 
                                                          IsChecked="{Binding IsSelected,UpdateSourceTrigger=PropertyChanged}"
                                              Margin="2"/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>

                        <TextBlock Foreground="{DynamicResource Brush.Foreground}" FontSize="14" Text="المعدات المتوفرة" Margin="0,15,0,5"/>
                        <ListBox ItemsSource="{Binding EquipmentsList}" SelectionMode="Multiple" Background="Transparent" BorderThickness="0">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="2" IsItemsHost="True"/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <ToggleButton Content="{Binding Name}" 
                                              Style="{DynamicResource MyChipToggleStyle}" 
                                                          IsChecked="{Binding IsSelected,UpdateSourceTrigger=PropertyChanged}"
                                              Margin="2"/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                    </StackPanel>
                </ScrollViewer>

            </Border>

            <Border Grid.Column="1" Grid.Row="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <ListBox Background="Transparent"   HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" BorderThickness="0" SelectionMode="Multiple" Grid.Row="2" ItemsSource="{Binding ExercisesList}" ItemContainerStyle="{StaticResource NewExercisesListBoxItemContainerStyle}">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="3" />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                    </ListBox>
                </Grid>
            </Border>



        </Grid>
    </Border>
</Window>
