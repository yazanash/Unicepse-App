<UserControl x:Class="Uniceps.Views.RoutineTemplateViews.RoutineComponent.RoutineItemsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Uniceps.Views.RoutineTemplateViews.RoutineComponent"
             mc:Ignorable="d" 
             FlowDirection="RightToLeft"
             xmlns:icons="http://metro.mahapps.com/winfx/xaml/iconpacks"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="DataGridDropRowStyle" TargetType="{x:Type DataGridRow}">
            <Setter Property="Background" Value="{DynamicResource Brush.Surface}"/>
            <Setter Property="Foreground" Value="{DynamicResource Brush.Foreground}"/>
            <Setter Property="BorderThickness" Value="{DynamicResource BorderThickness.Main}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource  Brush.Border}"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Margin" Value="0 4 0 0"/>
            <Setter Property="SnapsToDevicePixels" Value="true"/>
            <EventSetter Event="MouseMove" Handler="MouseMoveHandler"/>
            <EventSetter Event="DragOver" Handler="DragOverHandler"/>
            <EventSetter Event="Drop" Handler="ListBoxItem_Drop"/>
            <Setter Property="Validation.ErrorTemplate" Value="{x:Null}"/>
            <Setter Property="ValidationErrorTemplate">
                <Setter.Value>
                    <ControlTemplate>
                        <TextBlock Foreground="Red" Margin="2,0,0,0" Text="!" VerticalAlignment="Center"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridRow}">
                        <Border x:Name="DGR_Border" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}"   CornerRadius="{DynamicResource Radius.Small}" SnapsToDevicePixels="True">
                            <SelectiveScrollingGrid>
                                <SelectiveScrollingGrid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </SelectiveScrollingGrid.ColumnDefinitions>
                                <SelectiveScrollingGrid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </SelectiveScrollingGrid.RowDefinitions>
                                <DataGridCellsPresenter Grid.Column="1" ItemsPanel="{TemplateBinding ItemsPanel}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                <DataGridDetailsPresenter Grid.Column="1" Grid.Row="1" SelectiveScrollingGrid.SelectiveScrollingOrientation="{Binding AreRowDetailsFrozen, ConverterParameter={x:Static SelectiveScrollingOrientation.Vertical}, Converter={x:Static DataGrid.RowDetailsScrollingConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" Visibility="{TemplateBinding DetailsVisibility}"/>
                                <DataGridRowHeader Grid.RowSpan="2" SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical" Visibility="{Binding HeadersVisibility, ConverterParameter={x:Static DataGridHeadersVisibility.Row}, Converter={x:Static DataGrid.HeadersVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"/>
                            </SelectiveScrollingGrid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource Brush.Card}"/>
                </Trigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsSelected" Value="true"/>
                        <Condition Property="Selector.IsSelectionActive" Value="false"/>
                    </MultiTrigger.Conditions>
                    <Setter Property="BorderBrush" Value="{DynamicResource Brush.Primary}"/>
                    <Setter Property="Background" Value="{DynamicResource Brush.Card}"/>
                </MultiTrigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsSelected" Value="true"/>
                        <Condition Property="Selector.IsSelectionActive" Value="True"/>
                    </MultiTrigger.Conditions>
                    <Setter Property="BorderBrush" Value="{DynamicResource Brush.Primary}"/>
                    <Setter Property="Background" Value="{DynamicResource Brush.Card}"/>
                </MultiTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Border Grid.ColumnSpan="2" Margin="10 0" Grid.Row="2" Style="{DynamicResource StaticCardBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.ColumnSpan="2" Grid.Column="0" Orientation="Horizontal" Margin="0 0 0 10">
                <TextBlock Text="التمارين الرياضية" Foreground="{DynamicResource Brush.Foreground}" FontSize="16"  Margin="0 0 8 0"/>
                <Button Style="{StaticResource AddBtn}" Command="{Binding OpenExercisesCommand}" Width="100">
                    <TextBlock Text="أضافة تمارين"/>
                </Button>
            </StackPanel>
            <Border  Grid.Row="2" Grid.Column="0"  >
            <DataGrid FlowDirection="RightToLeft" ItemsSource="{Binding RoutineItems}" 
CanUserReorderColumns="True" SelectedItem="{Binding SelectedRoutineItem}"
x:Name="PlayerDataGrid" Style="{DynamicResource DataGridStyle1}"   DragLeave="TodoItemList_DragLeave"
 DragOver="TodoItemList_DragOver" RowStyle="{StaticResource DataGridDropRowStyle}" AllowDrop="True"
CellStyle="{DynamicResource DataGridCellStyle1}" VirtualizingPanel.ScrollUnit="Pixel"
ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}">
                
                <DataGrid.Columns> 
                    <DataGridTextColumn Header="#" IsReadOnly="True"
Width="auto"
Binding="{Binding Id}"
CanUserResize="False"/>

                    <DataGridTemplateColumn Header="الصورة" Width="auto" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Image Width="100"
                                 Source="{Binding ExerciseImage}"
                                 RenderOptions.BitmapScalingMode="HighQuality"
                                 RenderOptions.EdgeMode="Aliased"
                                 Height="100" DockPanel.Dock="Top"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="اسم التمرين" IsReadOnly="True"
 Binding="{Binding ExerciseName}"
Width="auto"
/>
                    <DataGridTemplateColumn  Width="auto" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" x:Name="menu">
                                    <Button Command="{Binding DeleteCommand}"  Style="{StaticResource DeleteBtn}">
                                        <icons:PackIconMaterial Kind="TrashCan"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            </Border>
        </Grid>
    </Border>

</UserControl>
