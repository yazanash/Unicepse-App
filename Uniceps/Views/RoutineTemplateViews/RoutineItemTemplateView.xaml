<UserControl x:Class="Uniceps.Views.RoutineTemplateViews.RoutineItemTemplateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Uniceps.Views.RoutineTemplateViews"
             mc:Ignorable="d" 
             xmlns:routinecomponent="clr-namespace:Uniceps.Views.RoutineTemplateViews.RoutineComponent"
                xmlns:icons="http://metro.mahapps.com/winfx/xaml/iconpacks"
                FlowDirection="RightToLeft" Background="#EEE"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <routinecomponent:DayGroupListView Grid.ColumnSpan="2" DataContext="{Binding RoutineDayGroupViewModel}"/>
        <routinecomponent:RoutineItemsView Grid.Row="1" Grid.Column="0" DataContext="{Binding RoutineItemListViewModel}" />
        <routinecomponent:RoutineItemModelCardView Grid.Row="2" Grid.Column="1" DataContext="{Binding SetModelListItemViewModel,UpdateSourceTrigger=PropertyChanged}" />

    </Grid>
</UserControl>
