<UserControl x:Class="Uniceps.Views.RoutineTemplateViews.CreateRoutineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Uniceps.Views.RoutineTemplateViews"
             mc:Ignorable="d" 
             FlowDirection="RightToLeft"
             d:DesignHeight="450" d:DesignWidth="800">
    <Border CornerRadius="10" Background="#EEE" Padding="10" >
      



            <Grid Grid.Row="1" >

            <Border Background="#fff" CornerRadius="10" Grid.Column="0"  Grid.Row="0"  Padding="5"  Margin="5">

                <DockPanel  HorizontalAlignment="Center" >
                    <DockPanel DockPanel.Dock="Top" >
                        <TextBlock Text="أضافة برنامج رياضي" Foreground="{StaticResource colors.primary}" FontSize="24" DockPanel.Dock="Left"/>


                    </DockPanel>
                    <Grid >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="5">
                            <TextBlock Text="اسم البرنامج" 
                                 Foreground="{StaticResource colors.secondry}"
                                 Margin="0 0 0 8"
                                 FontSize="18"/>
                            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource textBoxStyle}"/>

                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="1" Margin="5">
                            <TextBlock Text="المستوى" 
                                 Foreground="{StaticResource colors.secondry}"
                                 Margin="0 0 0 8"
                                 FontSize="18"/>
                            <TextBox Text="{Binding Level, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource textBoxStyle}"/>


                        </StackPanel>



                        <StackPanel Orientation="Horizontal" Grid.Row="5" Grid.Column="1" Margin="0 20 20 0" HorizontalAlignment="Left">
                            <Button Command="{Binding SubmitCommand}" Style="{StaticResource FormButton}" Width="200" Grid.Column="0" Grid.Row="4"  Content="حفظ"  HorizontalAlignment="Right">
                            </Button>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Grid.Row="5" Grid.Column="0" Margin="0 20 20 0" HorizontalAlignment="Right">
                            <Button Command="{Binding CancelCommand}" Style="{StaticResource ScFormButton}" Width="200"  Content="الغاء"/>
                        </StackPanel>
                    </Grid>

                </DockPanel>

            </Border>
        </Grid>
    </Border>
</UserControl>
