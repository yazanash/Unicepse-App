<UserControl x:Class="Uniceps.Views.RoutineTemplateViews.CoachDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Uniceps.Views.RoutineTemplateViews"
             mc:Ignorable="d" 
              xmlns:Comp="clr-namespace:Uniceps.utlis.Components"
             xmlns:icons="http://metro.mahapps.com/winfx/xaml/iconpacks"
             FlowDirection="RightToLeft"
             xmlns:Converters="clr-namespace:Uniceps.utlis.Converters"
               xmlns:cs="clr-namespace:Uniceps.Components"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Converters:EnumDisplayNameConverter x:Key="EnumToText"/>
        <Converters:InverseBooleanToVisibalityConverter x:Key="InverseBooleanToVisibalityConverter"/>
    </UserControl.Resources>
    <Border Padding="5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="1"  Padding="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="250" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="auto" />
                        </Grid.ColumnDefinitions>
                        <Border CornerRadius="{DynamicResource Radius.Small}" Margin="10 5"  Grid.Row="0"  Grid.Column="0"  VerticalAlignment="Top">
                            <cs:CustomTextBox Hint="ابحث هنا"  DataContext="{Binding SearchBox,UpdateSourceTrigger=PropertyChanged}"/>
                        </Border>
                        <StackPanel Grid.Row="0" Grid.Column="1" Margin="5" Orientation="Horizontal"  VerticalAlignment="Center">
                            <ComboBox Style="{DynamicResource ComboBoxStyle1}" 
                               ItemsSource="{Binding RoutineLevels,UpdateSourceTrigger=PropertyChanged}"
                               SelectedItem="{Binding SelectedLevel,UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"
                               ItemContainerStyle="{DynamicResource ComboBoxItemContainerStyle1}"
                                      Tag="المستوى :">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Converter={StaticResource EnumToText}}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>
                        <Button Content="أضافة برنامج" Command="{Binding AddRoutineCommand}" Grid.Column="3" Style="{StaticResource AddBtn}">
                           
                        </Button>
                    </Grid>

                    <Border Style="{DynamicResource StaticCardBorderStyle}" Grid.Row="2">
                        <DataGrid Grid.Row="2" FlowDirection="RightToLeft" ItemsSource="{Binding RoutineList}" SelectedItem="{Binding SelectedRoutine,UpdateSourceTrigger=PropertyChanged}"
               CanUserReorderColumns="True"
               x:Name="PlayerDataGrid" Style="{DynamicResource DataGridStyle1}" 
               CellStyle="{DynamicResource DataGridCellStyle1}" 
               ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" RowStyle="{DynamicResource DataGridRowStyle1}" Grid.ColumnSpan="3">

                            <DataGrid.Columns>
                                <DataGridTextColumn Header="#" IsReadOnly="True"
               Width="auto"
               Binding="{Binding Id}"
               CanUserResize="False"/>
                                <DataGridTemplateColumn Header="اسم البرنامج" Width="auto" IsReadOnly="True">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Name}" FontSize="16"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTextColumn Header="المستوى" IsReadOnly="True"
 Binding="{Binding Level, Converter={StaticResource EnumToText}}"
Width="auto"
/>

                                <DataGridTemplateColumn  Width="*" IsReadOnly="True">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" x:Name="menu">
                                                <Button Command="{Binding OpenProfileCommand}" Style="{StaticResource GridremoveBtn}">
                                                    <TextBlock Text="عرض البرنامج" FontSize="12"/>
                                                </Button>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                    <Comp:CaptinUniForEmpty Visibility="{Binding HasData,Converter={StaticResource InverseBooleanToVisibalityConverter}}" Grid.Row="2" Title="لا يوجد برامج رياضية بعد" 
                        Caption="اضغط على زر اضافة برنامج تجيز البرامج الرياضية للاعبين"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
