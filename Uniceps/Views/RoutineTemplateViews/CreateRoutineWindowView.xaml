<Window x:Class="Uniceps.Views.RoutineTemplateViews.CreateRoutineWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Uniceps.Views.RoutineTemplateViews"
        mc:Ignorable="d"
        xmlns:Converters="clr-namespace:Uniceps.utlis.Converters"
        FlowDirection="RightToLeft"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource Brush.Background}"
        Title="انشاء برنامج رياضي جديد" Height="350" Width="400">
    <Window.Resources>
        <Converters:EnumDisplayNameConverter x:Key="EnumToText"/>
    </Window.Resources>
    <Border Padding="5" >
        <Grid Grid.Row="1" >

            <Border Style="{DynamicResource CardBorderStyle}" Grid.Column="0"  Grid.Row="0"  Padding="5">

                <DockPanel   >
                    <DockPanel DockPanel.Dock="Top" >
                        <TextBlock Text="أضافة برنامج رياضي" Foreground="{DynamicResource  Brush.Foreground}" FontSize="24" DockPanel.Dock="Left"/>


                    </DockPanel>
                    <Grid >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="5">
                            <TextBlock Text="اسم البرنامج" 
                                Margin="0 0 0 8" Foreground="{DynamicResource  Brush.Foreground}"
                                FontSize="18"/>
                            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource textBoxStyle}"/>

                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="1" Margin="5">
                            <TextBlock Text="المستوى" 
                                Margin="0 0 0 8" Foreground="{DynamicResource  Brush.Foreground}"
                                FontSize="18"/>
                            <ComboBox Style="{DynamicResource ComboBoxStyle1}" 
  ItemsSource="{Binding RoutineLevels,UpdateSourceTrigger=PropertyChanged}"
  SelectedItem="{Binding Level,UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"
  ItemContainerStyle="{DynamicResource ComboBoxItemContainerStyle1}"
>
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Converter={StaticResource EnumToText}}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>


                        </StackPanel>



                        <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="1" Margin="0 10"  HorizontalAlignment="Center">
                            <Button Command="{Binding SubmitCommand}" Style="{StaticResource FormButton}" Width="200" Grid.Column="0" Grid.Row="4"  Content="حفظ"  HorizontalAlignment="Right">
                            </Button>
                        </StackPanel>
                       
                    </Grid>

                </DockPanel>

            </Border>
        </Grid>
    </Border>
</Window>
