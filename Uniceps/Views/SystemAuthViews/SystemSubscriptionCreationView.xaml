<UserControl x:Class="Uniceps.Views.SystemAuthViews.SystemSubscriptionCreationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Uniceps.Views.SystemAuthViews"
             mc:Ignorable="d" 
             FlowDirection="RightToLeft"
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Padding="5" >
        
            <Grid >
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="اشترك الان واحصل على جميع مميزات التي يقدمها نظامنا" 
               FontSize="24" FontWeight="Bold" Foreground="{DynamicResource Brush.Foreground}"
               HorizontalAlignment="Center" Margin="0 10"/>

                <ListBox Background="Transparent" ItemsSource="{Binding SystemPlans}" SelectedItem="{Binding SelectedPlan}" ItemContainerStyle="{StaticResource SystemSubscriptionListBoxItemContainerStyle1}" Grid.Row="1" BorderThickness="0">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                </ListBox>
            <Border Style="{DynamicResource StaticCardBorderStyle}" HorizontalAlignment="Center" Width="600"   Grid.Row="2" Margin="5 10" Padding="10">

                <ListBox  ItemsSource="{Binding Features}" ItemContainerStyle="{StaticResource PlanFeatureListBoxItemContainerStyle1}" Background="Transparent" BorderThickness="0">
                   
                </ListBox>
            </Border>
            <Button Grid.Row="3" Content="اشترك الآن"  Style="{StaticResource FormButton}" Width="200"
            Height="40" HorizontalAlignment="Center" 
            Command="{Binding CreateSystemSubscriptionCommand}"  />
            </Grid>
    </Border>
</UserControl>
