<UserControl x:Class="Uniceps.Views.PremiumView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Uniceps.Views"
             mc:Ignorable="d" 
             FlowDirection="RightToLeft"
             Background="{DynamicResource Brush.Background}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid >
        <Border HorizontalAlignment="Center" VerticalAlignment="Center" Padding="10" >
            <Grid >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel  VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10" Width="300">
                    <TextBlock FontSize="18" Margin="0 0 0 10" Foreground="{DynamicResource Brush.Foreground}" Text="هذه الميزة خاصة بالنسخة المدفوعة"/>
                    <TextBlock FontSize="16" Foreground="{DynamicResource Brush.Foreground}" TextWrapping="Wrap" Text="هل تحتاج هذه الميزة ؟ ..... اشترك واحصل على جميع الميزات"/>
                </StackPanel>
                <Image Grid.Column="1" Source="/Resources/Assets/CaptinUni/CaptinTablet.png" Height="300" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
