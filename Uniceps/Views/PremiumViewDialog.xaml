<Window x:Class="Uniceps.Views.PremiumViewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Uniceps.Views"
        mc:Ignorable="d"
        FlowDirection="RightToLeft"
        Background="{DynamicResource Brush.Background}"
        Width="500" Height="250"
        WindowStartupLocation="CenterScreen"
        Title="تنبيه" >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Image Source="/Resources/Assets/CaptinUni/CaptinOk.png" Height="200"/>
        <StackPanel Grid.Column="1" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="5" Width="250">
            <TextBlock FontSize="16" Margin="0 0 0 10" Foreground="{DynamicResource Brush.Foreground}" Text="يبدو ان عملك يتطور بسرعة"/>
            <TextBlock Margin="5 0" Foreground="{DynamicResource Brush.Foreground}" TextWrapping="Wrap" Text="لقد وصلت للحد الاعلى من الاستخدام .... اشترك بالنسخة المدفوعة واحصل على تسجيلات غير محدودة"/>
        </StackPanel>
        <Button Click="Button_Click" Style="{DynamicResource AddBtn}" Grid.Column="1" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="15" Width="70" Content="حسنا"/>

    </Grid>
</Window>
