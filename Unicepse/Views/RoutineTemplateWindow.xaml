<Window x:Class="Unicepse.Views.RoutineTemplateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Unicepse.Views"
        mc:Ignorable="d"
        Title="RoutineTemplateWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel x:Name="ExpanderPanel" Grid.Column="0" Margin="10">
            <ItemsControl ItemsSource="{Binding GroupList}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Expander Header="{Binding Name}">
                            <ListBox  ItemsSource="{Binding Exercises}"  AllowDrop="False">
                                
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border MouseMove="ListBoxItem_MouseMove" BorderThickness="1" x:Name="Bd" BorderBrush="#aaa" Margin="5" Padding="5" CornerRadius="10" Background="#fff">
                                            <DockPanel >
                                                <Image Width="200"
            Source="{Binding ExerciseImage}"
             RenderOptions.BitmapScalingMode="HighQuality"
RenderOptions.EdgeMode="Aliased"
            Height="112" DockPanel.Dock="Top"/>
                                                <StackPanel Orientation="Vertical" Margin="5" DockPanel.Dock="Top" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding ExerciseName}" Margin="0 0 0 5" HorizontalAlignment="Left" FontSize="14" />

                                                </StackPanel>
                                               

                                            </DockPanel>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Expander>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <!-- StackPanel for daily lists -->
        <ScrollViewer Grid.Column="1" HorizontalScrollBarVisibility="Visible">
            
        <StackPanel x:Name="DailyListsPanel" Orientation="Horizontal" Grid.Column="1" Margin="10">
            <ItemsControl ItemsSource="{Binding DailyLists}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Day}" FontWeight="Bold" Margin="5"/>
                                <ListBox ItemsSource="{Binding Exercises}" ItemContainerStyle="{StaticResource AddRoutineItemsListBoxItemContainerStyle1}" AllowDrop="True" Drop="ListBox_Drop" Width="200" Height="300" Margin="5">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}"/>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
        </ScrollViewer>

        <Button Grid.Column="2" VerticalAlignment="Top" Height="30" Width="50" Command="{Binding AddDailyListCommand}" Content="AddDay"></Button>
    </Grid>
</Window>
